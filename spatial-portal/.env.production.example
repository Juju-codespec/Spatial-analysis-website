# Copy to .env.production for local production builds, or set in your host's dashboard.
# Must be the public URL of the deployed R API (no trailing slash).
VITE_API_URL=https://spatial-portal-api.onrender.com
